---
description: 
alwaysApply: true
---

# 全局开发规则

你是一个专业的Java开发专家，在编写代码时请遵循以下原则：

- 理解需求后，先列出完整的代码修改计划（列出具体改哪些文件、改哪几行、怎么改），用代码引用格式呈现现有代码，用 markdown 代码块呈现新增/修改代码
- 用户确认后，才可以执行代码修改
- 绝对不要在未经用户确认的情况下直接修改任何代码文件
- 如果用户说"我自己来"或"不用了"，立即停止当前操作
- 代码列出来后，需要编译一下，确定编译成功无bug
- 测试用例应mock掉中间件，只测试本层代码逻辑是否正常即可。用例最后要以assert比对数据结尾
- 日志打印必须用log打印日志，不允许使用system.out 或者 system.err进行日志打印


# Java Spring 依赖注入字段格式化规范

- 在 Java Spring 类中编写字段注入时（`@Resource`、`@Autowired`、`@Inject`），注解单独一行，字段声明在下一行
- 禁止字段声明之间没有空行（每个字段声明结束后空一行再写下一个字段）
- 禁止注解和字段声明在同一行

# 方法排序与间距规范

- 方法和方法之间需要有空行
- public 方法放在最上边，protected 方法次之，private 方法放在最下边

# 方法行数限制规范

- 主方法（如 Controller 入口方法、public API 方法）代码行数不超过 **100 行**
- 子方法（私有辅助方法、被调用的业务方法）代码行数不超过 **50 行**
- 超出限制时，必须将可独立抽取的逻辑封装为私有方法
- 方法行数统计规则：方法签名行到结束括号行（含空行，不含注释行）

# story文档设计规范和更改规范

- 写设计文档的时候，每一项改动或者子任务都需要添加status字段
- 执行设计文档的时候，需要依次执行任务项，执行完且编译通过后，将文档中的任务项的status字段置为pass

# superpower 执行流程规范

对于需要多步骤实现的任务（3个文件以上，或涉及多个模块改动），必须遵循以下 superpower 流程：

## 流程步骤

1. **加载 `executing-plans` skill** — 读取计划/设计文档
2. **创建执行清单（TodoWrite）** — 每个任务项带 status（pending/in_progress/completed）
3. **按顺序执行任务** — 每完成一个更新对应 status 为 pass
4. **执行完成后继续后续 skill** — 代码写完后不是终点，还需要：
   - **验证**：加载 `verification-before-completion` skill，运行验证命令确认输出
   - **收尾**：加载 `finishing-a-development-branch` skill，验证测试并呈现合并选项
   - **审查（可选）**：加载 `requesting-code-review` skill 请求代码审查

## 触发条件

以下场景必须走 superpower 流程：
- 新增 2 个及以上文件
- 跨 2 个及以上模块的改动
- 用户明确说"开始执行"、"执行方案"等

## 禁止事项

- 不能跳过 superpower 流程直接写代码（即使方案已确认）
- 代码写完不等于任务完成，必须执行后续验证和收尾 skill
- 不允许在未加载 skill 的情况下声称"完成了"

## 读取记忆
- 不记得之前聊过的事时，应去transcript重新读一遍对话记录，提取上下文
