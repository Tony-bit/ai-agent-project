<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="denny.ai.agent.infrastructure.dao.IAiClientAdvisorDao">

    <resultMap id="AiClientAdvisorMap" type="denny.ai.agent.infrastructure.dao.po.AiClientAdvisorPO">
        <id column="id" property="id"/>
        <result column="advisor_id" property="advisorId"/>
        <result column="advisor_name" property="advisorName"/>
        <result column="advisor_type" property="advisorType"/>
        <result column="order_num" property="orderNum"/>
        <result column="ext_param" property="extParam"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientAdvisorPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_client_advisor (advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time)
        VALUES (#{advisorId}, #{advisorName}, #{advisorType}, #{orderNum}, #{extParam}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientAdvisorPO">
        UPDATE ai_client_advisor
        <set>
            <if test="advisorId != null">advisor_id = #{advisorId},</if>
            <if test="advisorName != null">advisor_name = #{advisorName},</if>
            <if test="advisorType != null">advisor_type = #{advisorType},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="extParam != null">ext_param = #{extParam},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="queryById" parameterType="java.lang.Long" resultMap="AiClientAdvisorMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE id = #{id}
    </select>

    <select id="queryByAdvisorId" parameterType="java.lang.String" resultMap="AiClientAdvisorMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE advisor_id = #{advisorId}
    </select>

    <select id="queryList" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientAdvisorPO" resultMap="AiClientAdvisorMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        <where>
            <if test="advisorId != null and advisorId != ''">AND advisor_id = #{advisorId}</if>
            <if test="advisorType != null and advisorType != ''">AND advisor_type = #{advisorType}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY order_num ASC, create_time DESC
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_client_advisor WHERE id = #{id}
    </delete>

</mapper>

