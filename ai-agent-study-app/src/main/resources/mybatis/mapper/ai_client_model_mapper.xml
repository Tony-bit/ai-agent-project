<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="denny.ai.agent.infrastructure.dao.IAiClientModelDao">

    <resultMap id="AiClientModelMap" type="denny.ai.agent.infrastructure.dao.po.AiClientModelPO">
        <id column="id" property="id"/>
        <result column="model_id" property="modelId"/>
        <result column="api_id" property="apiId"/>
        <result column="model_name" property="modelName"/>
        <result column="model_type" property="modelType"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientModelPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_client_model (model_id, api_id, model_name, model_type, status, create_time, update_time)
        VALUES (#{modelId}, #{apiId}, #{modelName}, #{modelType}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientModelPO">
        UPDATE ai_client_model
        <set>
            <if test="modelId != null">model_id = #{modelId},</if>
            <if test="apiId != null">api_id = #{apiId},</if>
            <if test="modelName != null">model_name = #{modelName},</if>
            <if test="modelType != null">model_type = #{modelType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="queryById" parameterType="java.lang.Long" resultMap="AiClientModelMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time
        FROM ai_client_model
        WHERE id = #{id}
    </select>

    <select id="queryByModelId" parameterType="java.lang.String" resultMap="AiClientModelMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time
        FROM ai_client_model
        WHERE model_id = #{modelId}
    </select>

    <select id="queryList" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientModelPO" resultMap="AiClientModelMap">
        SELECT id, model_id, api_id, model_name, model_type, status, create_time, update_time
        FROM ai_client_model
        <where>
            <if test="modelId != null and modelId != ''">AND model_id = #{modelId}</if>
            <if test="apiId != null and apiId != ''">AND api_id = #{apiId}</if>
            <if test="modelType != null and modelType != ''">AND model_type = #{modelType}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_client_model WHERE id = #{id}
    </delete>

</mapper>

