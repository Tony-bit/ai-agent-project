<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="denny.ai.agent.infrastructure.dao.IChatSessionDao">

    <resultMap id="ChatSessionMap" type="denny.ai.agent.infrastructure.dao.po.ChatSessionPO">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="user_id" property="userId"/>
        <result column="agent_id" property="agentId"/>
        <result column="client_id" property="clientId"/>
        <result column="message_count" property="messageCount"/>
        <result column="first_query" property="firstQuery"/>
        <result column="last_response" property="lastResponse"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="denny.ai.agent.infrastructure.dao.po.ChatSessionPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_session (
            session_id, user_id, agent_id, client_id, message_count,
            first_query, last_response, status, create_time, update_time
        ) VALUES (
            #{sessionId}, #{userId}, #{agentId}, #{clientId}, #{messageCount},
            #{firstQuery}, #{lastResponse}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateLastResponse">
        UPDATE ai_chat_session
        SET last_response = #{lastResponse},
            message_count = message_count + #{messageCountIncrement},
            update_time = NOW()
        WHERE session_id = #{sessionId}
    </update>

    <select id="queryBySessionId" resultMap="ChatSessionMap">
        SELECT id, session_id, user_id, agent_id, client_id, message_count,
               first_query, last_response, status, create_time, update_time
        FROM ai_chat_session
        WHERE session_id = #{sessionId}
    </select>

</mapper>
