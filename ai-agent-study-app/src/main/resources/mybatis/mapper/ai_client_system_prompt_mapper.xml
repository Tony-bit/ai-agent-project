<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="denny.ai.agent.infrastructure.dao.IAiClientSystemPromptDao">

    <resultMap id="AiClientSystemPromptMap" type="denny.ai.agent.infrastructure.dao.po.AiClientSystemPromptPO">
        <id column="id" property="id"/>
        <result column="prompt_id" property="promptId"/>
        <result column="prompt_name" property="promptName"/>
        <result column="prompt_content" property="promptContent"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="prompt_type" property="promptType"/>
        <result column="version" property="version"/>
        <result column="change_desc" property="changeDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientSystemPromptPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_client_system_prompt (
        prompt_id, prompt_name, prompt_content, description, status,
        prompt_type, version, change_desc, create_time, update_time
        ) VALUES (
        #{promptId}, #{promptName}, #{promptContent}, #{description}, #{status},
        #{promptType}, #{version}, #{changeDesc}, now(), now()
        )
    </insert>

    <update id="updateById" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientSystemPromptPO">
        UPDATE ai_client_system_prompt
        <set>
            <if test="promptId != null and promptId != ''">prompt_id = #{promptId},</if>
            <if test="promptName != null and promptName != ''">prompt_name = #{promptName},</if>
            <if test="promptContent != null and promptContent != ''">prompt_content = #{promptContent},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="promptType != null">prompt_type = #{promptType},</if>
            <if test="version != null">version = #{version},</if>
            <if test="changeDesc != null">change_desc = #{changeDesc},</if>
            update_time = now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByPromptId" parameterType="denny.ai.agent.infrastructure.dao.po.AiClientSystemPromptPO">
        UPDATE ai_client_system_prompt
        <set>
            <if test="promptName != null and promptName != ''">prompt_name = #{promptName},</if>
            <if test="promptContent != null and promptContent != ''">prompt_content = #{promptContent},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="promptType != null">prompt_type = #{promptType},</if>
            <if test="version != null">version = #{version},</if>
            <if test="changeDesc != null">change_desc = #{changeDesc},</if>
            update_time = now()
        </set>
        WHERE prompt_id = #{promptId}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_client_system_prompt WHERE id = #{id}
    </delete>

    <delete id="deleteByPromptId" parameterType="java.lang.String">
        DELETE FROM ai_client_system_prompt WHERE prompt_id = #{promptId}
    </delete>

    <select id="queryById" parameterType="java.lang.Long" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE id = #{id}
    </select>

    <select id="queryByPromptId" parameterType="java.lang.String" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_id = #{promptId} AND status = 1
    </select>

    <select id="queryEnabledPrompts" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="queryByPromptName" parameterType="java.lang.String" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_name LIKE CONCAT('%', #{promptName}, '%')
        ORDER BY create_time DESC
    </select>

    <select id="queryAll" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        ORDER BY create_time DESC
    </select>

    <!-- 精确查生效版本（核心方法） -->
    <select id="queryActiveByPromptIdAndType" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_id = #{promptId}
          AND prompt_type = #{promptType}
          AND status = 1
        LIMIT 1
    </select>

    <!-- 查所有历史版本 -->
    <select id="queryVersionHistory" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_id = #{promptId}
          AND prompt_type = #{promptType}
        ORDER BY version DESC
    </select>

    <!-- 原子激活版本（单条 CASE WHEN，并发安全） -->
    <update id="activateVersion">
        UPDATE ai_client_system_prompt
        SET status = CASE WHEN id = #{id} THEN 1 ELSE 0 END,
            update_time = NOW()
        WHERE prompt_id = #{promptId}
          AND prompt_type = #{promptType}
    </update>

    <!-- 批量查指定 promptType 的所有生效记录 -->
    <select id="queryActivePromptsByPromptType" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_type = #{promptType}
          AND status = 1
    </select>

    <select id="queryVersionSet" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_type = #{promptType}
          AND version = #{version}
          AND prompt_id IN
          <foreach collection="promptIds" item="promptId" open="(" separator="," close=")">
              #{promptId}
          </foreach>
    </select>

    <select id="queryActiveSet" resultMap="AiClientSystemPromptMap">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status,
               prompt_type, version, change_desc, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_type = #{promptType}
          AND status = 1
          AND prompt_id IN
          <foreach collection="promptIds" item="promptId" open="(" separator="," close=")">
              #{promptId}
          </foreach>
    </select>

    <update id="deactivatePromptSet">
        UPDATE ai_client_system_prompt
        SET status = 0, update_time = NOW()
        WHERE prompt_type = #{promptType}
          AND prompt_id IN
          <foreach collection="promptIds" item="promptId" open="(" separator="," close=")">
              #{promptId}
          </foreach>
    </update>

    <update id="activatePromptSetVersion">
        UPDATE ai_client_system_prompt
        SET status = 1, update_time = NOW()
        WHERE prompt_type = #{promptType}
          AND version = #{version}
          AND prompt_id IN
          <foreach collection="promptIds" item="promptId" open="(" separator="," close=")">
              #{promptId}
          </foreach>
    </update>

</mapper>
